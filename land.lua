-- always configure the library key settings properly
local lib = require '❤️.library' ('land', 'functional')

-- function called when the state is entered
function lib:load(hen, cfg)

end

-- function called when the state is exited
function lib:unload(hen)

end

return lib
